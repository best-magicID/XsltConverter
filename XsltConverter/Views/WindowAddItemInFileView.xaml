<Window x:Class="XsltConverter.Views.WindowAddItemInFileView"
        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:vm="clr-namespace:XsltConverter.ViewModels"
        
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:WindowAddItemInFileViewModel, IsDesignTimeCreatable=True}"
        Title="Добавление нового Item" 
        Height="200" 
        Width="300" 
        WindowStartupLocation="CenterScreen" 
        SizeToContent="Height">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin"
                    Value="5"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Margin"
                    Value="5"/>
            <Setter Property="Height"
                    Value="auto"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin"
                    Value="5"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Имя (name)"
                   Grid.Column="0"
                   Grid.Row="0"/>
        <TextBox Grid.Column="1"
                 Grid.Row="0"
                 Text="{Binding NewEmployee.Name}"/>
        
        <!--<ComboBox Grid.Column="1"
                  Grid.Row="0"
                  ItemsSource="{Binding }"
                  IsReadOnly="False"/>-->

        <TextBlock Text="Фамилия (surname)"
                   Grid.Column="0"
                   Grid.Row="1"/>
        <TextBox Grid.Column="1"
                 Grid.Row="1" 
                 Text="{Binding NewEmployee.SurName}"/>
        <!--<ComboBox Grid.Column="1"
                  Grid.Row="1"
                  ItemsSource="{Binding }"/>-->

        <TextBlock Text="Сумма (amount)"
                   Grid.Column="0"
                   Grid.Row="2"/>
        <TextBox Grid.Column="1"
                 Grid.Row="2"
                 Text="{Binding NewEmployee.Amount}"/>

        <TextBlock Text="Месяц (mount)"
                   Grid.Column="0"
                   Grid.Row="3"/>
        <ComboBox Grid.Column="1"
                  Grid.Row="3"
                  ItemsSource="{Binding ListMonths}"
                  SelectedItem="{Binding NewEmployee.Month}"/>

        <Button Content="Добавить"
                Name="ButtonAddItem"
                Margin="5"
                Grid.Column="1"
                Grid.Row="4"
                Command="{Binding AddItemCommand}"/>
    </Grid>
</Window>
